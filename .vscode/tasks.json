{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build JAR",
            "type": "java (buildArtifact)",
            "mainClass": "App",
            "targetPath": "${workspaceFolder}/target/${workspaceFolderBasename}.jar",
            "elements": [
                "${workspaceFolder}/bin",
                "${workspaceFolder}/lib"
            ],
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
